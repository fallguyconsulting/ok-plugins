# Decision catalog (auto-generated)

Read first. Then either grep for the matching annotation
(`@concept:` / `@story:` / `@decision:`) in the code under
review, or read `decisions/<slug>.md` for the full body. Generated
by `discover-design` and refreshed whenever a sprint's
deltas touch the catalog. Do not edit by hand — changes will
be overwritten.

## Decisions

- `append-only-issue-queue` — Issues live in a single append-only JSON-lines event log: one object per line, three event shapes (open, and the term...
- `code-cites-design` — The direction of reference between code and the design corpus is fixed one-way: code carries kind-plus-slug annotatio...
- `comments-forbidden-by-default` — Under the lint methodology, comments are not permitted in source files by default.
- `content-addressed-src-tag` — The artifact tag is derived by hashing the entire working tree — uncommitted changes included — through a temporary i...
- `declared-stack-profile` — Plugins whose discipline varies by stack split the pipeline into three: a detection scan proposes a profile from repo...
- `edit-hook-blocks-in-turn` — Lint enforcement runs as a post-edit hook that blocks the agent in the same turn on violations — but only within the ...
- `falsifier-exhibition` — A proof counts as passing only when its declared falsifier has been exhibited in this run: the falsifying mutation ap...
- `filesystem-discovery-markers` — "Which suite plugins does this project use" is answered solely by checking for each plugin's committed dot-directory ...
- `hook-shims` — Every hook file in a plugin is a shim with one job: resolve the project root, exec the same-named materialized hook i...
- `lockstep-suite-version` — Every plugin manifest carries the same semantic version, bumped together at the highest level any plugin's changes wa...
- `no-execution-engine` — The planner ships no execution machinery and defines no plan artifact: a sprint is never rewritten into a plan, stagi...
- `per-project-pinning` — Every materialized artifact — scripts, hooks, cheatsheets, the vendored lint binary — is stamped with the writing plu...
- `prove-audit-audience-split` — The two corpus-checking verbs have disjoint audiences and channels: the proof run produces work items for an agent — ...
- `ratchet-over-soft-start` — A project whose violation backlog is too large to clear at once records a baseline count, and CI fails any change tha...
- `relevance-scoped-queue-gate` — A feature-work planning session drafts the sprint first; a dedicated relevance reviewer then splits the open issues i...
- `single-source-transclusion` — Every canonical definition, template, and rule the planner's skills share lives in one shared definitions file (plus ...
- `slash-only-activation` — Every user-facing skill declares in its description that it is activated only by its explicit slash command and never...
- `teardown-gates-in-git-flags` — Workspace close verifies its two gates (clean tree, merged branch) and then tears down using only the non-forcing for...
- `whole-file-ownership` — A plugin owns whole files only — version-stamped, deterministically regenerable, overwritten wholesale — and never ed...
- `worktrees-inside-project-root` — Job worktrees are created by default inside the plugin's dot-directory under the project root, and because a checkout...
