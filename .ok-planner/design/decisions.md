# Decision catalog (auto-generated)

Read first. Then either grep for the matching annotation
(`@concept:` / `@story:` / `@decision:`) in the code under
review, or read `decisions/<slug>.md` for the full body. Generated
by `discover-design` and refreshed whenever a sprint's
deltas touch the catalog. Do not edit by hand — changes will
be overwritten.

## Decisions

- `adversarial-implementation-audits` — Whether the project supports what an artifact claims is determined by a per-artifact `implementation:` verdict — `sup...
- `affirmative-practices-over-exemptions` — A project's coding policy is expressed as practices over named subjects, every one of them affirmative. A site that d...
- `affirmative-warrant-ladder` — A documentation claim is recorded as *held* only on an affirmative warrant: a passing experiment driven through the p...
- `audit-audience-split` — The audit writes its own records and nothing else: the per-artifact determinations of the two axes it checks, into th...
- `closing-commit-baseline` — When a certification close-out archives a sprint and commits the work, it stamps the archived sprint file's frontmatt...
- `code-cites-design` — The direction of reference between code and the design corpus is fixed one-way: code carries kind-plus-slug annotatio...
- `cold-boxed-synthesis` — Assumptions are formed inside the audit run, after the story determinations land: a single synthesizer agent whose in...
- `comments-forbidden-by-default` — Under the lint methodology, comments are not permitted in source files by default. Exactly three structural exemption...
- `content-addressed-src-tag` — The artifact tag is derived by hashing the working tree the repository's own committed ignore rules admit — uncommitt...
- `declared-stack-profile` — Plugins whose discipline varies by stack split the pipeline into three: a detection scan proposes a profile from repo...
- `document-composes-audit` — `/document` opens by ensuring a current audit at the release commit — composing the audit as its own skill, never abs...
- `documentation-citations-are-product` — Documentation-corpus records carry citations, and every citation means "at the stamped commit" — but the shipped laye...
- `documentation-walk-in-composed-audit` — The set of document types is settled in a **documentation walk**: a short owner conversation that reads the run's sur...
- `documents-generated-per-type-and-placed` — After it constructs the records, `/document` runs a **Generate** step: one writer per declared document type, briefed...
- `edit-hook-blocks-in-turn` — Lint enforcement runs as a post-edit hook that blocks the agent in the same turn on violations — but only within the ...
- `event-kinds-as-conventioned-strings` — The suite carries one events standard beside the testing standard, materialized in the plumbline estate with a cheats...
- `filesystem-discovery-markers` — "Which suite families does this project use" is answered solely by checking for each family's committed dot-directory...
- `final-form-deltas` — A corpus delta carries the complete final-form artifact body, resolved fully during the planning ceremony, and execut...
- `full-reassessment-per-release` — Every audit run re-derives and re-verifies every conclusion: the assumption set is synthesized fresh, every story-way...
- `lockstep-suite-version` — Every plugin manifest carries the same semantic version at every release, bumped together at the highest level any ch...
- `no-execution-engine` — The planner ships no execution machinery and defines no plan artifact: a sprint is never rewritten into a plan, stagi...
- `owner-guided-surface-partition` — The project's public surface is captured in one prose document committed to the estate — the **surface intent** per `...
- `per-project-pinning` — Every materialized artifact — vendored skills, scripts, hooks, cheatsheets, the vendored lint binary — is stamped wit...
- `placed-documents-are-records` — A document the ceremony places in the tree is a **record** in the estate's sense, though it lives outside the estate:...
- `ratchet-over-soft-start` — A project whose violation backlog is too large to clear at once records a baseline count in a budget file inside the ...
- `relevance-scoped-queue-gate` — A feature-work planning session drafts the sprint first; a dedicated relevance reviewer then splits the unruled open ...
- `single-source-transclusion` — Every canonical definition, template, and rule the planner's skills share is defined exactly once, in one shared dire...
- `slash-only-activation` — Every user-facing skill declares in its description that it is activated only by its explicit slash command and never...
- `steering-over-prose-lint` — The writing standard is enforced by steering, through four channels. Its portable dispatch rule is injected into the ...
- `subagent-model-follows-job` — Every dispatch — the Agent tool and every `agent()` call in a Workflow script — names one of `opus`, `sonnet`, or `ha...
- `suite-owned-ceremonies` — Planning, certification, audit, and documentation are suite-owned verbs rather than any family's: one canonical body ...
- `task-tools-mirror-the-report` — `/ok` offers, as a consented settings transcription beside the hook entry, the project-scoped environment setting tha...
- `team-execution-cold-gate` — The sprint's execution shape names a team. The session orchestrates it and never joins it as a worker: it relays mess...
- `teardown-gates-in-git-flags` — Workspace close verifies its two gates (clean tree, merged branch) and then tears down using only the non-forcing for...
- `test-quality-by-review` — The suite carries one testing standard, materialized in the plumbline estate as a document of its own, with the cheat...
- `user-vantage-story-audits` — Story support is determined from the user's vantage: the audit verifies each story by driving the released product th...
- `vendored-skills` — Everything project-scoped the suite delivers — skill files, hook implementations, support scripts, context payloads, ...
- `violations-are-remediation-not-issues` — A site that departs from the practice governing it is remediation work carried by ordinary planning, never an entry i...
- `whole-file-ownership` — The suite's machinery — the front door's administration and every family's converge core — owns whole files only: ver...
- `worktrees-inside-project-root` — Job worktrees are created by default inside the plugin's dot-directory under the project root, and because a checkout...
