# Concept catalog (auto-generated)

Read first. Then either grep for the matching annotation
(`@concept:` / `@story:` / `@decision:`) in the code under
review, or read `concepts/<slug>.md` for the full body. Generated
by `discover-design` and refreshed whenever a sprint's
deltas touch the catalog. Do not edit by hand — changes will
be overwritten.

## Concepts

- `annotation` — An annotation is an in-source marker linking code back to a design-corpus artifact by kind and slug — a concept enfor...
- `assessment` — An assessment is a documentation-corpus record capturing one measured way a promise or prior played out against a rel...
- `assumption` — An assumption is a user-vantage prior: something a user would take to be true about a product before anyone checks, w...
- `catalog-toc` (aliases: catalog, TOC) — A catalog table of contents is the generated one-file index beside each durable catalog: an alphabetical list of slug...
- `cheatsheet` (aliases: rules layer) — A cheatsheet is the one suite-owned file each integrated skill family maintains in the consumer's always-in-context r...
- `citation-tag` — A citation tag is a project-declared comment form the lint methodology permits: a tag paired with a structural resolu...
- `completion-contract` — The completion contract is the fixed, verbatim stop condition carried in every sprint, every term verifiable from the...
- `completion-report` — The completion report is a sprint execution's durable record: one file beside the sprint document, named for it, that...
- `concept-artifact` (aliases: concept) — A concept is the design-corpus artifact kind that defines a load-bearing noun the system traffics in — general and ab...
- `conduct` (aliases: ok-conduct) — The conduct is the suite's optional behavioral layer, shipped as its own user-scoped plugin: an output style layered ...
- `content-addressed-tag` (aliases: src-tag) — A content-addressed tag is an artifact identifier derived from the exact content of the working tree, including uncom...
- `corpus-delta` (aliases: delta) — A corpus delta is one unit of change to the design corpus as carried inside a sprint: a final-form artifact body — a ...
- `decision-artifact` (aliases: decision, TD, technical decision) — A decision is the design-corpus artifact kind that records a real architectural or technical choice: one shape adopte...
- `design-corpus` (aliases: design docs, durable design docs, the corpus) — The design corpus is a project's durable, committed model of what it is and what it owes its users, held at a fixed a...
- `document-type` — A document type is an owner-authored artifact naming one document a release ships: what the document is for, which cl...
- `documentation-corpus` — The documentation corpus is the artifact set the documentation ceremony produces at a release, stamped with the relea...
- `estate` (aliases: dot-directory, project-side estate) — An estate is a skill family's committed project-side presence, rooted in one dot-directory named for the family: decl...
- `experiment` — An experiment is a small runnable artifact that checks one assumption or one story-way by driving the released produc...
- `finding` — A finding is one defect surfaced by any of the suite's review passes — compliance review, coverage and drift checks, ...
- `integration-contract` — The integration contract is the suite's normative spine: the single set of conventions by which every skill family me...
- `issue` — An issue is anything about the design corpus that requires human judgment to resolve — sloppy, unspecified, unclear, ...
- `materialized-artifact` (aliases: vendored binary, materialization) — A materialized artifact is a project-side copy of a canonical file the suite carries — a family's, or the suite's own...
- `practice` — A practice is an affirmative statement of what a codebase does about some members of a subject: what the code is, the...
- `sketch` — A sketch is a pre-commitment design artifact: an idea captured in one pass, in enough detail to think about it, share...
- `skill` — A skill is one named prompt file whose markdown body is executable substance: process steps, embedded subagent prompt...
- `skill-family` (aliases: family) — A skill family is the suite's unit of project-scoped distribution: a self-contained directory of skills, templates, s...
- `sprint` — A sprint is the planning ceremony's terminal artifact: a change-order against the design corpus, expressed as final-f...
- `stack-profile` (aliases: profile) — A stack profile is a plugin's committed, owner-declared description of the consumer project's stack — the languages, ...
- `story-artifact` (aliases: story) — A story is the design-corpus artifact kind that records a durable user expectation — what the product owes its users ...
- `subject` — A subject is a named, enumerable population of constructs in a codebase that a project has something to say about. It...
- `surface-extraction` — The surface extraction is the enumerated index of user-facing elements the audit produces per run, at `.ok-planner/au...
- `surface-intent` — The surface intent is a project's single prose document that captures what its user-facing surface is meant to be. It...
- `trap` — A trap is an assumption a user would reasonably hold, contradicted by the product's actual behavior at a release — re...
- `true-up` — True-up is the suite's administration act: the idempotent converge of a project's suite presence — the ceremony verbs...
- `workspace` — A workspace is one job's isolated place of work: its own checkout on its own branch under profile-declared naming, pl...
