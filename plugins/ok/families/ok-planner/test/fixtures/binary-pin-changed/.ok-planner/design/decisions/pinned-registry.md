---
decision: pinned-registry
---

# The service registry is a compiled artifact, pinned whole

## Choice

The registry the gateway enumerates is a compiled binary artifact, not
a text table: an audit that quantifies over its members pins the whole
file, because no line of it can be anchored.
