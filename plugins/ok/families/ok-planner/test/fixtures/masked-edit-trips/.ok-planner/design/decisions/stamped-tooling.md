---
decision: stamped-tooling
---

# Materialized tooling is stamped with the suite version

## Choice

Every materialized artifact carries the suite version: the cheatsheet
stamp line, the session hook's banner, the vendored script's header,
the executable's VERSION assignment, and the plugin manifest's version
field. The stamps change on every release and on nothing else.
