# Materialized by ok-planner v{{OK_PLANNER_VERSION}}. Suite-owned:
# overwritten wholesale by the front door's administration (`/ok`).
#
# Generated, machine-local estate content: real on disk and read freely,
# but never content of the repository — and so never reachable by the
# source graph either, which asks git what is ignored before it walks.
# Everything else in the estate stays tracked.

# The corpus view's build, fetched to match the suite version this
# estate is stamped with.
browser/

# Machine-local run state — the pid and port the browse script records
# for a corpus view it started, so a later `down` can stop it.
run/

