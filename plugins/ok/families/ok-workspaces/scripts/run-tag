#!/bin/sh
# Materialized by ok-workspaces v{{OK_WORKSPACES_VERSION}} — suite-owned; overwritten on converge; do not hand-edit.

set -eu
printf 'run-%s\n' "$(od -An -N6 -tx1 /dev/urandom | tr -d ' \n')"
